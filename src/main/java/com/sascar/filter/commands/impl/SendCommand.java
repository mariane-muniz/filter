package com.sascar.filter.commands.impl;

public class SendCommand {
    /**
     * Name of the topic that receive the filtered events
     * @return String
     */
//    String destinationTopic();

    /**
     * List of key values that send in the event for the filtered topic.
     * Case the list returned null, all available fields are send to the filtered topic.
     * @return String
     */
//    List<String> fields();

    /**
     * Validate if exist any configuration available for the data.
     * Eg.: client with identification 134
     * @param dto All positions received in Sascar
     * @return Boolean
     */
//    Boolean validate(final Position dto);
}
